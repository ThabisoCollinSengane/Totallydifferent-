SUPABASE_URL=https://ekmgnrnlhksqywdmftcs.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=
APP_URL=https://totallydifferent.vercel.app
RESEND_API_KEY=
RESEND_FROM=Totallydifferent <hello@totallydifferent.co.za>

# Admin dashboard (/admin) — username + password login. The service key never touches the browser.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
# Optional: secret used to sign admin session tokens. Falls back to SUPABASE_SERVICE_KEY if unset.
ADMIN_SESSION_SECRET=

# Optional: error monitoring. Set to your Sentry DSN to enable server-side error
# reporting (api/shared.js → captureError). Leave blank to log to stderr only.
SENTRY_DSN=
